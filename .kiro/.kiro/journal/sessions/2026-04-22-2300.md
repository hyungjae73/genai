# Session: 2026-04-22 23:00

## Summary
CI test-frontend の `@vitest/coverage-v8` MISSING DEPENDENCY エラーが rerun 後も再発。原因は `actions/cache` が古い `node_modules` を復元し、`npm ci` が毎回削除→再インストールするため非効率かつキャッシュ不整合が発生していた。キャッシュヒット時に `npm ci` をスキップする条件分岐を追加。

## Tasks Completed
- deploy.yml の test-frontend キャッシュ戦略を修正: `cache-hit` 判定で `npm ci` をスキップする条件分岐追加

## Decisions Made
- **CI キャッシュ戦略変更**: `actions/cache` に `id: npm-cache` を付与し、`cache-hit != 'true'` の場合のみ `npm ci` を実行。キャッシュヒット時は復元済み `node_modules` をそのまま使用。

## Open Items
- deploy.yml 変更が未コミット・未プッシュ
- テスト11ファイル失敗の修正（既存問題、今回は未対応）

## Related Specs
- `.kiro/specs/production-readiness-improvements/` — CI/CD関連
