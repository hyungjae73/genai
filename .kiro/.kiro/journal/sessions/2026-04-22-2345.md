# Session: 2026-04-22 23:45

## Summary
CI test-backend の passlib/crypt DeprecationWarning エラーを修正。passlib を完全排除し bcrypt 直接使用に移行。CI test-frontend の ESLint は 0 errors / 62 warnings で通過確認。

## Tasks Completed
- `src/auth/password.py`: passlib → bcrypt 直接使用に移行（Python 3.11 crypt 非推奨対応）
- `requirements.txt`: `passlib[bcrypt]==1.7.4` を削除
- `pytest.ini`: crypt DeprecationWarning をフィルタに追加（安全策）
- `vite.config.ts`: `coverage.provider: 'v8'` 明示設定（前セッション分）
- `deploy.yml`: setup-node 組み込み npm キャッシュに移行（前セッション分）

## Decisions Made
- **passlib 完全排除**: Python 3.11+ で `crypt` モジュールが deprecated → passlib がインポート時に DeprecationWarning → pytest `filterwarnings=error` でCI失敗。bcrypt ライブラリ直接使用で根本解決。

## Open Items
- 変更が未コミット・未プッシュ
- テスト11ファイル失敗（既存問題、カバレッジ/passlib とは無関係）

## Related Specs
- `.kiro/specs/user-auth-rbac/` — パスワードハッシュ関連
- `.kiro/specs/production-readiness-improvements/` — CI/CD関連
