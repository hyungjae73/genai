# Session: 2026-04-22 23:30

## Summary
CI `@vitest/coverage-v8` MISSING DEPENDENCY エラーの根本原因を特定・修正。`vite.config.ts` に `coverage.provider: 'v8'` を明示設定し、deploy.yml のキャッシュ戦略を `node_modules` 直接キャッシュから `setup-node` 組み込みの `~/.npm` キャッシュに変更。

## Tasks Completed
- `vite.config.ts` に `coverage: { provider: 'v8' }` を追加
- deploy.yml: `actions/cache` による `node_modules` キャッシュを廃止、`setup-node` の `cache: npm` に移行
- ローカルで `npx vitest --run --coverage` が MISSING DEPENDENCY なしで動作することを確認

## Decisions Made
- **CI npm キャッシュ戦略**: `node_modules` を直接キャッシュするのではなく、`setup-node` の `cache: npm` で `~/.npm` をキャッシュし、`npm ci` は常に実行する方式に統一。これにより `package-lock.json` の変更が確実に反映される。
- **coverage provider 明示化**: vitest の自動検出に頼らず `provider: 'v8'` を明示設定。

## Open Items
- deploy.yml + vite.config.ts の変更が未コミット・未プッシュ
- テスト11ファイル失敗（既存問題、カバレッジとは無関係）

## Related Specs
- `.kiro/specs/production-readiness-improvements/` — CI/CD関連
