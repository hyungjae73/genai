# Session: 2026-04-14 18:00

## Summary
ログイン失敗の根本原因を特定。passlib 経由の bcrypt ハッシュ生成で不正なハッシュ（31文字未満）が生成されていた。bcrypt ライブラリを直接使って正しいハッシュ（60文字）を生成し DB に書き込んで解決。curl でのログインは成功確認済み。ブラウザでの失敗は古い refresh_token Cookie の干渉が原因と推定。

## Tasks Completed
- bcrypt ライブラリで正しいハッシュを生成（`$2b$12$...` 60文字）
- DB に直接 UPDATE で正しいハッシュを書き込み
- curl でログイン成功を確認（200 OK、access_token 取得）
- CORS ヘッダーも正常動作を確認

## Decisions Made
- **passlib の bcrypt 互換性問題を回避**: 今後のパスワードリセットは `bcrypt` ライブラリを直接使う方針

## Topics Discussed
- passlib の bcrypt ハッシュ生成で `ValueError: malformed bcrypt hash (checksum must be exactly 31 chars)` が発生
- 正しい bcrypt ハッシュは60文字（`$2b$12$` + 53文字）
- ブラウザでの失敗は古い `refresh_token` Cookie が干渉している可能性

## Open Items
- ブラウザの Cookie クリア（DevTools → Application → Cookies → 削除）またはシークレットウィンドウでの再試行をユーザーに案内済み

## Related Specs
- `.kiro/specs/user-auth-rbac/`（認証システム）
