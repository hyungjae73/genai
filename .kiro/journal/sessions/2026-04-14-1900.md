# Session: 2026-04-14 19:00

## Summary
審査キュー取得 500 エラーを修正。`review/service.py` の `list_reviews()` で `func.case()` を使っていたが、SQLAlchemy 2.x では `case()` を直接インポートして使う必要がある。修正後 200 OK を確認。

## Tasks Completed
- `genai/src/review/service.py` の `func.case()` → `from sqlalchemy import case` + `case()` に修正

## Decisions Made
- 特になし（バグ修正のみ）

## Topics Discussed
- SQLAlchemy 2.x では `func.case()` が廃止され `case()` を直接使う必要がある
- 307 リダイレクト → 500 の順で問題を特定（URL末尾スラッシュ → CASE式の構文エラー）

## Open Items
- なし

## Related Specs
- `.kiro/specs/manual-review-workflow/`
