# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
alembic==1.13.0
psycopg2-binary==2.9.9

# Task Queue
celery==5.3.4
redis==5.0.1

# Web Scraping
playwright==1.40.0
playwright-stealth==1.0.6
beautifulsoup4==4.12.2
# NOTE: lxml 6.1.0 fixes GHSA-vfmq-68hx-4jfw but requires Python >=3.10.
# On Python 3.9, mitigate by using resolve_entities='internal' in all parsers.
lxml==4.9.4

# NLP and Text Processing
scikit-learn==1.5.2

# Notifications
sendgrid==6.11.0
slack-sdk==3.26.1

# Security
cryptography==46.0.7
# NOTE: pyjwt 2.12.0 fixes GHSA-752w-5fwx-jx9f (crit header bypass) but requires Python >=3.10.
# On Python 3.9, 2.11.0 is the latest available. Mitigate by validating crit headers in app code.
pyjwt==2.11.0
bcrypt==4.1.2
email-validator==2.1.0

# OpenTelemetry
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-exporter-otlp-proto-grpc==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-instrumentation-sqlalchemy==0.43b0
opentelemetry-instrumentation-httpx==0.43b0
opentelemetry-instrumentation-celery==0.43b0

# Retry
tenacity==8.2.3

# Utilities
# NOTE: python-dotenv 1.2.2 fixes GHSA-mf9w-mj56-hr94 (symlink attack) but requires Python >=3.10.
# On Python 3.9, 1.1.1 is the latest available. Low risk if .env files are not writable by untrusted users.
python-dotenv==1.1.1
jinja2==3.1.6
# NOTE: python-multipart 0.0.22+ fixes path traversal (GHSA-wp53-j4wj-2cfg) and 0.0.26+
# fixes preamble DoS (GHSA-mj87-hwqh-73pj), but both require Python >=3.10.
# 0.0.20 fixes the ReDoS (GHSA-2jv5-9r88-3w3p) and excessive logging DoS (GHSA-59g5-xgcq-4qw3).
python-multipart==0.0.20
httpx==0.25.2

# OCR
pytesseract==0.3.10
Pillow==10.4.0
pdf2image==1.16.3
numpy>=1.24.0

# Testing
pyyaml==6.0.1
pytest==8.3.5
pytest-asyncio==0.21.1
pytest-cov==4.1.0
hypothesis==6.92.1
faker==20.1.0
testcontainers[postgres]>=4.0.0
fakeredis[lua]>=2.20.0

# Development
black==24.10.0
flake8==6.1.0
mypy==1.7.1
